apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - repository.yaml
  - release.yaml
configMapGenerator:
  - name: openebs-values
    namespace: openebs
    files:
      - values.yaml=openebs-values.yaml
configurations:
  - kustomizeconfig.yaml
