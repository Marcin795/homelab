apiVersion: v1
kind: Service
metadata:
  name: plex-service
  namespace: default
  labels:
    app: plex
spec:
  ports:
    - port: 32400
      targetPort: 32400
      name: http
  selector:
    app: plex
