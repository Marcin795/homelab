apiVersion: v1
kind: PersistentVolume
metadata:
  name: plex-config
  namespace: arr
  labels:
    app.kubernetes.io/name: plex
    app: plex
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /var/disk/a/data/arr/plex
    type: DirectoryOrCreate
