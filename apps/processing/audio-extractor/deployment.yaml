apiVersion: apps/v1
kind: Deployment
metadata:
  name: audio-extractor
  namespace: processing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: audio-extractor
  template:
    metadata:
      labels:
        app: audio-extractor
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: audio-extractor
          image: ghcr.io/marcin795/processing-audio_extractor:latest
          imagePullPolicy: Always
          envFrom:
            - secretRef:
                name: rabbit-credentials
          volumeMounts:
            - mountPath: /data
              name: data
      volumes:
        - name: data
          hostPath:
            path: /var/storage/processing/data
            type: DirectoryOrCreate
