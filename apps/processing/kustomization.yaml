apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - rabbit
  - ingest
  - download
secretGenerator:
  - name: rabbit-secret
    namespace: processing
    envs:
      - rabbit.env
    options:
      disableNameSuffixHash: true
